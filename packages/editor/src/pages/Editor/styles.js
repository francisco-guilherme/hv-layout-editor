import { createStyles } from "@material-ui/core";

const styles = () =>
  createStyles({
    root: {
      display: "flex",
      minHeight: "calc(100vh - 54px)",
      marginTop: 4
    },
    canvas: {}
  });

export default styles;
