import { createStyles } from "@material-ui/core";

const styles = theme =>
  createStyles({
    root: {},
    buttons: {
      position: "absolute",
      top: 20,
      right: 20
    },
  });

export default styles;
