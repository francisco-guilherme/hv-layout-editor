import { createStyles } from "@material-ui/core";

const styles = () =>
  createStyles({
    root: {
      display: "flex",
      alignItems: "center",
      justifyContent: "center"
    },
    switch: {
      marginRight: 20
    }
  });

export default styles;
